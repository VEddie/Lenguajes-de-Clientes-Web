const defaultMatrix = [1, 4, 8, 9, 2, 7, 3, 5, 4];

let parseMatrix = (arr, size) => {
    let result = [];
    let aux = [];

    for(let i = 0; i < size; i++) {
        aux.push(arr.shift())    
    }
}